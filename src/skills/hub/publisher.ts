/**
 * Skill publisher (Phase 3)
 */
export class SkillPublisher {
    async publish(_skillDir: string, _registryUrl: string): Promise<void> {
        // Phase 3
        console.log('Skill publishing not yet available');
    }
}
